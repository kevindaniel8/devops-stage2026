---
# Dépendances Ansible pour le cluster PostgreSQL repmgr
collections:
  - name: community.postgresql
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.4.0"

roles:
  - name: geerlingguy.postgresql
    version: ">=3.0.0"
    source: galaxy
